import { FunctionAlias,CallSignature } from "./Interfaces/ITypeCall";

const typedFunctionAlias: FunctionAlias = (input) => input.length;
const typedCallSignature: CallSignature = (input) => input.length;